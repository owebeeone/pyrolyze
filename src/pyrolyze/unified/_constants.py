"""Environment and defaults for the unified native library."""

from __future__ import annotations

# Public so apps, tests, and docs can agree on the variable name without import cycles.
PYROLYZE_UNIFIED_BACKEND_ENV = "PYROLYZE_UNIFIED_BACKEND"

DEFAULT_UNIFIED_BACKEND = "qt"
